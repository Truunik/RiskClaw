{
  "name": "@riskclaw/agents",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "loop": "bun run src/run-loop.ts",
    "demo:explain": "bun run src/scripts/demo-explain.ts",
    "compute:list": "bun run src/scripts/compute-list-services.ts",
    "compute:ledger": "bun run src/scripts/compute-ledger-status.ts",
    "compute:fund-and-test": "bun run src/scripts/compute-fund-and-test.ts",
    "storage:smoke": "bun run src/scripts/storage-smoke.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@0gfoundation/0g-ts-sdk": "^1.2.6",
    "@0glabs/0g-serving-broker": "^0.7.5",
    "ethers": "^6.13.1",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
